# Ensure devDependencies (typescript + all @types/* packages) are installed
# even when NODE_ENV=production is set (Render's build default).
# Without this, `npm install` on Render omits devDependencies and `npm run build` (tsc) fails.
include=dev
